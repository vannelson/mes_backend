name: FTP Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.1.0
        with:
          server: ftp.knp.edu.ph
          port: 21
          username: ftp_mes_backend@knp-api.online
          password: Eulerfermat143!
          local-dir: './'
          server-dir: '/'
          exclude: |
            **/.env
            **/vendor/**
            **/node_modules/**
